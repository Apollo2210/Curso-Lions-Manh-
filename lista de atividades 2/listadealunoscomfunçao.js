const prompt = require("prompt-sync")();
function cadastraralunos() {
  let alunos = [" Dantas ", " Luiza "];
  let novoalunodasala = prompt(" Qual o nome do novo aluno da sala ? R: ");
}
let novoalunodasala = cadastraralunos();

if (cadastraralunos.leght =3 ) {
    console.log(" Turma formada com sucesso " + " Alunos: " + cadastraralunos.alunos + " ! ")
}